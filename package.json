{
  "name": "smart-dark-pdf-reader",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "playwright test",
    "test:all": "vitest run && playwright test",
    "serve": "python3 -m http.server 8000"
  },
  "devDependencies": {
    "vitest": "^3.2.1",
    "happy-dom": "^18.0.1",
    "@playwright/test": "^1.52.0",
    "pdf-lib": "^1.17.1"
  }
}
